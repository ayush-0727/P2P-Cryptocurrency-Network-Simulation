## Install the required python dependencies by using
    pip install -r requiremnts.txt

## Run the Simulator by using
    python main.py --n <nodes> --Ttx <Ttx> --z0 <slow nodes %> --z1 <low cpu nodes %>